spring.application.name=Interview Hippo
spring.datasource.url=jdbc:mysql://localhost:3306/interview_hippo?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=man2MAN2!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

debug=true

# TODO use environmental variables to ensure security when actually deploying

# Existing properties...

# Enable debug logging for Spring
logging.level.org.springframework=TRACE

# Enable debug logging for your application package
logging.level.com.example.interviewhippo=DEBUG

# Show details of errors in the browser
server.error.include-stacktrace=always
server.error.include-message=always

# Disable Whitelabel Error Page
server.error.whitelabel.enabled=false

spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
